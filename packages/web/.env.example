# Registry API base URL used by the resolver (server-side fetch) and the JSON envelope route.
AGENID_API_URL=http://localhost:3001
# Public origin of this site; used to build absolute links in badge.js and metadata.
NEXT_PUBLIC_SITE_URL=https://agenid.org
